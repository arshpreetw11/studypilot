# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# Comma-separated origins allowed to call the API (only needed if the frontend is hosted separately)
ALLOWED_ORIGINS=*
